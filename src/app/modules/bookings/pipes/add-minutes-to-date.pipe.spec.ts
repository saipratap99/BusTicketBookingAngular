import { AddMinutesToDatePipe } from './add-minutes-to-date.pipe';

describe('AddMinutesToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AddMinutesToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
